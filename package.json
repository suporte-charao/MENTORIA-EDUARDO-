{
  "name": "metodo-charao-pre-inscricao",
  "private": true,
  "description": "Landing de pré-inscrição do Método Charão (site estático)",
  "scripts": {
    "dev": "live-server --port=8080 --no-css-inject --watch=index.html,assets,favicon.ico"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
